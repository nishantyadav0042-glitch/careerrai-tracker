# 0C.3 — Duplication Map

**Status:** READ-ONLY AUDIT. No code, schema, migration, test or documented
behaviour changed. Nothing here is implemented.
**Date:** 23 Aug 2026. Verified against `main` @ `f3a5063`.
**Companion:** `docs/0C.3-CANONICAL-FACT-MIGRATION.md` (the reasoning, the
blockers, the proposed waves). This file is the flat table the mandate asked for.

**Legend — one classification per implementation, exactly as the mandate defines:**
KEEP · MIGRATE · DELETE · ADAPTER · DIFFERENT FACT · AMBIGUOUS (stop, escalate).

---

## Fact 1–2 — `syllabus_coverage_units` / `syllabus_coverage_pct`

Authority for the *predicate*: `src/lib/coverage-status.ts` → `isCovered()`.
Authority for the *fact*: **does not exist.** Every consumer below counts it itself.

| Fact | Authority | Duplicate | Location | Difference | Action | Status |
|---|---|---|---|---|---|---|
| coverage_units | `isCovered()` | `.filter(isCovered).length` | `api/blueprint/route.ts:105` | universe = `topicMemory` (all 46, missing rows defaulted `not_started`) | MIGRATE | proposed |
| coverage_units | `isCovered()` | `.filter(isCovered).length` | `student/tracker/page.tsx:427` | universe = one section's `entries` — a *section* count wearing the syllabus name | MIGRATE → fact 3 | proposed |
| coverage_units | `isCovered()` | `totalTopics − remainingTopics` | `student/tracker/page.tsx:168` | **second formula**, never calls `isCovered`; equals it only because the ladder partitions cleanly | MIGRATE | proposed |
| coverage_units | `isCovered()` | `.filter(isCovered).length` | `lib/daily-insight.ts:235` | denominator is `topicMemory.length`, not 46 | MIGRATE | proposed |
| coverage_units | `isCovered()` | `.filter(isCovered).length` | `lib/buddy-briefing.ts:63` | universe = `topicMemory` | MIGRATE | proposed |
| coverage_units | `isCovered()` | `rows.filter(isCovered).length` | `components/buddy/cockpit.tsx:80` | **universe = raw `topic_coverage` rows** — only topics that already have a row. A student with 10 rows has a universe of 10 | MIGRATE | proposed |
| coverage_units | `isCovered()` | `if (isCovered) finished++` | `lib/sales-conversion.ts:86` | universe = `computeTopicMemory`; own per-section aggregate | MIGRATE | proposed |
| coverage_units | `isCovered()` | `rows.filter(isCovered).length` | `lib/student-brief.ts:82` | **reads `profiles.topic_matrix` (JSONB onboarding snapshot), not `topic_coverage`** | **DIFFERENT FACT** | needs a key |
| coverage_units | `isCovered()` | `m.status === 'practicing' \|\| m.status === 'revising'` | `student/onboarding/screens/screen-topic-coverage.tsx:315,322,343` | **drops `exam_ready`** — the 11th-implementation bug that `isCovered()` was written to end, still live in three lines | MIGRATE | proposed |
| coverage_pct | — none — | `Math.round((total−remaining)/total*100)` | `student/tracker/page.tsx:168` | by topic count over 46 | MIGRATE | proposed |
| coverage_pct | — none — | `Math.round(covered/rows.length*100)` | `lib/student-brief.ts:84` | over the onboarding matrix | DIFFERENT FACT | needs a key |
| coverage_pct | — none — | `Math.round(finished/total*100)` | `lib/sales-conversion.ts:93` | per section, over topics with metadata | MIGRATE → fact 3 | proposed |
| coverage_pct | — none — | `Math.round((inMotion/total)*100)` | `lib/prep-memory-data.ts:148` | numerator is **`inMotionTopics`, not covered** — counts `learning` too, labelled `knowledge` | DIFFERENT FACT | needs a key |
| denominator | `TOPIC_METADATA` | `Object.keys(TOPIC_METADATA).length` | `api/blueprint:92`, `student/tracker:129`, `admin/leads/[id]:186`, `lib/prep-memory-data:138` | four independent re-derivations of "46" | MIGRATE | proposed |
| denominator | `TOPIC_METADATA` | `all.length` | `api/coverage/weekly-review:66` | universe = rows returned by that query | AMBIGUOUS | escalate |
| denominator | `TOPIC_METADATA` | `ctx.bySection.reduce(...entries.length)` | `lib/prep-insight-engine.ts:748` | universe = the insight context's own grid | AMBIGUOUS | escalate |

## Fact 3–4 — `section_coverage_units` / `section_topics_remaining`

| Fact | Authority | Duplicate | Location | Difference | Action | Status |
|---|---|---|---|---|---|---|
| section_units | none | `all.filter(isCovered).length` per `['QA','VARC','DILR']` | `lib/buddy-briefing.ts:66-70` | section from `TOPIC_METADATA[t.topic]?.section` | MIGRATE | proposed |
| section_units | none | `agg[meta.section].finished++` | `lib/sales-conversion.ts:91` | seeds all three sections at 0, then filters `total>0` | MIGRATE | proposed |
| section_units | none | `entries.filter(isCovered).length` | `student/tracker/page.tsx:427` | section grid built by `computeTopicMemory` | MIGRATE | proposed |
| section_units | none | `rows.filter(isCovered).length` | `lib/student-brief.ts:82` | `profiles.topic_matrix` — different source | DIFFERENT FACT | needs a key |
| topics_remaining | none | `notStartedCount + learningCount` | `api/blueprint/route.ts:109` | `learning` counts as remaining | MIGRATE | proposed |
| topics_remaining | none | `notStartedCount + learningCount` | `student/tracker/page.tsx:135` | **byte-identical duplicate of the line above** | DELETE (import) | proposed |
| topics_remaining | none | `topicMemory.length − finished` | `lib/daily-insight.ts:236` | subtractive form; agrees today, drifts if a status is added | MIGRATE | proposed |
| topics_remaining | none | `untouchedN + inProg` | `student/tracker/page.tsx:433` | per-section, third naming of the same idea | MIGRATE | proposed |
| topics_remaining | none | `remainingSyllabusHours(rows, mult)` | `components/buddy/cockpit.tsx:84`, `lib/buddy-case-data.ts:69` | **hours, not topics** — a genuinely different unit | DIFFERENT FACT | KEEP as-is |

## Fact 5 — `logged_days_last_7`

The North Star's own input. Six producers, **two source tables**, **two window
lengths**, and one surface that renders the wrong one under the right name.

| Fact | Authority | Duplicate | Location | Difference | Action | Status |
|---|---|---|---|---|---|---|
| logged_days_7 | none | `new Set(report_date).size` over `.gte(today−7)` | `lib/weekly-diagnosis.ts:30,42` | **8-day window** rendered as `"Studied N of 7 days"` (line 130) — can print 8/7 | MIGRATE | **P0** |
| logged_days_7 | none | `logs.length` over `.gte(today−7)` | `api/weekly-signal/route.ts:62,71` | 8-day window; rendered `"{n}/7 days logged"` (line 16). Row-count is safe — `UNIQUE(student_id, report_date)` verified in `001_initial_schema.sql:45` | MIGRATE | **P0** |
| logged_days_7 | none | same `−7` pattern | `api/feedback-draft/route.ts:38,62,106` | 8-day window; rendered `"{n}/7 days logged"` | MIGRATE | **P0** |
| logged_days_7 | none | same `−7` pattern | `api/chat/draft/route.ts:57,93` | 8-day window; rendered `"{n}/7 days logged"` | MIGRATE | **P0** |
| logged_days_7 | none | `last7.length` over `cutoff.setDate(−7)` | `buddy/(dashboard)/students/[id]/page.tsx:66,70` | 8-day window; fires a mentor flag `"Only N/7 days logged"` | MIGRATE | **P0** |
| logged_days_7 | none | `reports.length` | `lib/analytics.ts:134` (`daysSubmitted`) | window is the caller's `period`; callers pass `−7` (`check-red-flags:24`, `weekly-digest:25`, `admin/students:46`, `admin/buddies/roster:32`) | MIGRATE | **P0** |
| logged_days_7 | none | `windowStats(completionRecords, …, addDays(today,−6), today)` | `lib/prep-memory-data.ts:89,113` | **correct 7-day window**, but source is `routine_task_completions` — "days with a tick", not "days with a log" | **DIFFERENT FACT** | needs `ticked_days_last_7` |
| logged_days_7 | none | `loggedDaysLast7: prepMemory.last7.daysStudied` | `admin/leads/[id]/page.tsx:169` | **names the tick fact as the log fact** — a mislabel on an operator surface | MIGRATE | **P0** |

## Fact 6 — `logged_today`

| Fact | Authority | Duplicate | Location | Difference | Action | Status |
|---|---|---|---|---|---|---|
| logged_today | none | `useQuery` → `hasLoggedToday` | `hooks/useLogging.ts:55` | client, server-hydrated | MIGRATE | proposed |
| logged_today | none | `days.has(today)` | `api/cron/study-companion/route.ts:142` | Set of `report_date`; gates 7 separate pushes (`:220,249,255,279,289,307,317`) | MIGRATE | proposed |
| logged_today | none | `loggedDays.size` ladder | `api/cron/daily-reminder/route.ts:76-136` | Set of `report_date`, graduation at `>= 7` | MIGRATE | proposed |
| logged_today | none | `loggedDays.size` | `api/cron/onboarding-morning/route.ts:48-60` | duplicate of the block above | DELETE (import) | proposed |
| logged_today | none | `new Set(logs.map(student_id)).size` | `api/admin/capability-health/route.ts:64` | **cohort**-scoped, not student-scoped | DIFFERENT FACT | KEEP as-is |
| logged_today | none | `isAlreadyPersisted` round-trip | `api/logging/log-daily:106,150` | not a metric — a write-confirmation read. G4 consumer #29 | KEEP | — |

## Fact 7 — `self_reported_baseline`

**BLOCKED — the registry and the shipped contract disagree.** See
`0C.3-CANONICAL-FACT-MIGRATION.md` §2. `src/lib/facts/contract.ts:15` names
this key as **"the rejected `self_reported_baseline`"** — an umbrella that
"re-admits duplication wearing a registry badge." The 0C.3 mandate re-lists it
as fact 7. No implementation may be classified until that is resolved.

---

## High-risk unregistered metrics — inventoried, NOT registered

Per the mandate: listed separately, no key proposed, no migration proposed.

| Metric | Distinct implementations | Authority today | Note |
|---|---|---|---|
| `study_duration` / actual hours | **30 consumers**, 6 writers | `study-duration-source.ts` (provenance only) | Fully mapped 18 Aug — `docs/0C-3G-G4-STUDY-DURATION-AUDIT.md`. Seven open blockers B1–B7, four unclassifiable consumers. **Not reopened here.** |
| `weakest_section` | 5 producers | `lib/focus-sections.ts` → `resolveFocusSections()` | Bypasses: `api/routine/add-block:62` and `api/blueprint:67` read `profiles.self_reported_weakest_section` raw, skipping the mock rung. `lib/student-brief.ts:89` derives it from coverage %. `lib/buddy-match.ts:50` and `admin/admin-match-panel.tsx:36` each rank baseline scores locally — `buddy-match.ts` holds **both** its own and the authority (`:250`) |
| `?? 'DILR'` fallback | 4 sites | `resolveFocusSections` records `weakestSource:'default'` | `add-block:62` and `blueprint:67` default **without recording that they defaulted**. `routine/today:290` and `routine-engine:443,590` are downstream of the authority — redundant, not a bypass |
| `streak` | not audited | `streak_data` + `upsert_log_and_streak` | out of scope this pass |
| `progress` | see facts 1–2 | — | |
| plan adherence / completion | not audited | `completion-portion.ts` | out of scope this pass |
| remaining hours | 2 | `remainingSyllabusHours()` | already single-authority; different unit from fact 4 |
| syllabus finish date | not audited | `projectSyllabusFinish()` + `plan-extension.ts` | Phase D is frozen; do not touch |

---

## What is already single-authority — do not "fix"

Recorded so a later wave does not manufacture work.

| Concept | Authority | Adoption |
|---|---|---|
| covered predicate | `coverage-status.ts:isCovered()` | 18 call sites; 3 bypass lines remain (onboarding) |
| coverage ladder / ranking | `coverage-status.ts:STATUS_ORDER` | complete |
| the study day | `lib/study-day.ts:studyDayString()` | 52 files. **05:30 IST ≡ 00:00 UTC by design**, so a plain `toISOString().slice(0,10)` returns the identical string — only **2** files construct a day key another way. Guard D is largely satisfied *by construction*; the live divergence is in **window arithmetic**, not day keys |
| focus / weakest resolution | `lib/focus-sections.ts` | 5 canonical callers, 2 real bypasses |
| plan assembly | `plan-day.ts:buildDayPlan()` → `resolveFocusSections()` | `routine/today:156`, `routine-plan.ts:192` |
| duration provenance | `study-duration-source.ts` | complete |
| truth boundary | `lib/truth/source.ts`, `batch.ts`, `mutation-gate.ts` | 1 site (`weekly-plan-reconcile`) — B3b will widen it |
