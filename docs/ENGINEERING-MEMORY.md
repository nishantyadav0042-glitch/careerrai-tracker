# CareerRai Engineering Memory

> **Permanent organizational knowledge.** Every production incident, outage,
> rollback, failed experiment, and architecture mistake becomes a durable entry
> here — so no engineer and no AI agent ever repeats it. This is not a changelog;
> it is the record of what went wrong, *why*, what it cost, and what now prevents
> it. **Before building in a subsystem, read the incidents that touch it.**
>
> **How to add an entry:** append with the next number. Be honest about impact
> and root cause — a sanitized memory teaches nothing. When the prevention is
> encoded in a Constitution, link it, so the lesson has teeth beyond this file.

---

## Index

| # | Date | Title | Area | Students hit |
|---|---|---|---|---|
| 1 | 2026-07 | Push subscriptions dying same-day | Notification | 34 |
| 2 | 2026-07-19 | Zero new students could log | Notification / Learning | full cohort (0/29) |
| 3 | 2026-07-21 | Dead video-session links | Trust | all buddy sessions |
| 4 | 2026-07 | Stale streaks (displayed ≠ real) | Learning / Analytics | all loggers |
| 5 | 2026-07-20 | Dashboard contradicted itself | Analytics | founder (decisions) |
| 6 | 2026-07 | 0-hour log excluded from streak | Learning | honest loggers |
| 7 | 2026-07 | Invented statistic nearly shipped | Trust | (caught pre-ship) |
| 8 | 2026-07-25 | PWA start_url redirected, installs silently failed | Growth | student-reported |
| 9 | 2026-07-25 | exam_ready was self-declarable | Trust / data | 6 students |
| 10 | 2026-07-28 | App Store rejection: unreachable login | Growth | launch-blocking |
| 11 | 2026-07-29 | Curated Daily Pick wearing a student's byline | Trust | all readers |
| 14 | 2026-08-01 | Security sweep revoked the one grant | Database access | ≥1 confirmed |
| 15 | 2026-08-01 | iOS payment fix sat on a branch for a day | Trust (payment) | every iOS student |
| 17 | 2026-08-04 | One meeting, two truths — mentor lost Join at T+0 | Trust | 1st orientation |
| 18 | 2026-08-04 | /welcome shipped with no login door | Growth | every returning user |
| 19 | 2026-08-05 | Play rejected listing — screenshots the guide had banned | Growth / store | launch-blocking |
| 20 | 2026-08-05 | Plan told a paying student to re-learn finished topics | Learning | every student |
| 21 | 2026-08-05 | A pair sent to two different video rooms | Trust | 1st paying student |
| 22 | 2026-08-06 | Five engines, one student, no two numbers the same | Analytics | 1 (visible) |
| 23 | 2026-08-06 | One file, three walls — a rule in N places drifts N−1 times | Architecture | — |
| 24 | 2026-08-06 | An instruction to a model is not a limit | AI | — |
| 25 | 2026-08-07 | A rule the founder killed kept running in three other files | Architecture | — |
| 26 | 2026-08-11 | Three planners, one Tuesday | Learning | all plan users |
| 27 | 2026-08-11 | The database moved, the code didn't — signups lost name+phone | Data / Signup | 20 (8 phones recovered) |
| 28 | 2026-08-12 | Whole Plan re-rolled the day the student was holding | Learning | 1 visible (all exposed) |
| 29 | 2026-08-12 | git reset --hard destroyed a verified change pre-commit | Process / agent | — (caught next day) |
| 30 | 2026-08-12 | Daily log rejected fractional hours — students could not log | Learning (P0) | all who marked a task "Half" |
| 31 | 2026-08-24 | A paid ₹299 student fell out of the lifecycle and no row was wrong | Trust (P0) | 1 of 2 paying students |
| 32 | 2026-08-26 | The test database is a scaffold, not a replica — probes certified a hole | Process / testing | — (caught pre-prod) |
| 33 | 2026-08-26 | `revoke from public` did not revoke it — Supabase grants anon/authenticated explicitly | Security | — (caught pre-prod) |
| 34 | 2026-08-26 | `claim_lead` is SECURITY DEFINER and callable by anon — *closed 2026-08-28* | Security (P1) | sales pipeline ownership |
| 35 | 2026-08-26 | Deploy took the whole site down (runner build lost NEXT_PUBLIC_SUPABASE_URL) | Deploy / Growth | full cohort (874) |
| 36 | 2026-08-27 | A cancelled session welded the ₹299 to a booking that would never happen | Trust (P0) | every future mentor cancellation |
| 37 | 2026-08-27 | Daily Insight repeated for days — the suppression read had no upper bound | Learning (P1) | every student who opened Home twice |
| 38 | 2026-08-27 | 71% of completions got the wrong topic — task ids are unique only within a day | Learning (P1) | every student the recovery rule fired for |
| 39 | 2026-08-27 | The guard read one of the two routes it named — a lost notification failed a committed booking | Notification / Trust (P1) | every mentor-booked session whose dispatch failed |
| 40 | 2026-08-28 | A refunded payment stayed `paid` forever — refunded money counted as revenue, and would have paid commission | Trust (money) (P1) | every refund ever processed; both counsellors from 2 Sept |
| 41 | 2026-08-28 | Fixing #40 removed the accident that blocked re-activation — a replayed capture after a refund would have handed premium back | Trust (money) (P0) | any refunded student inside Razorpay's retry window |
| 42 | 2026-08-28 | #41's fix was in the caller; the same defect sat in `activate_payment` SQL, and the profiles update had no guard at all | Trust (money) (P0) | any refunded student; reproduced before it shipped |
| 40 | 2026-08-27 | The rules were real, and attached to the wrong verb — availability was `before insert` only | Trust (P1) | any student whose session was moved |
| 41 | 2026-08-27 | A paid entitlement could be bypassed by the path the booking came through | Trust (P0, money) | every mentor-booked session for a paying student |
| 43 | 2026-08-29 | Google signup looped back into onboarding — the stash endpoint inverted its throttle and rejected every request ever made, and the claim sat in a branch a DB trigger made unreachable | Growth / Learning (P0) | every student who chose Continue with Google; 0 drafts stored in the feature's lifetime |
| 44 | 2026-08-29 | An invisible U+FEFF in `NEXT_PUBLIC_SUPABASE_URL` made `new URL()` reject it — 9 of 10 Google callbacks died on "PKCE code verifier not found", a message that never mentions a URL | Growth (P0) | every Google sign-in for a day; 3 accounts left with 0 coverage rows |
| 45 | 2026-08-29 | The OAuth `state` was encoded twice, so the callback's `indexOf(':')` found no colon and read the nonce as empty — every mentor Google Calendar connection ever attempted was refused as `state_mismatch` | Trust (P0) | all mentors; 0 rows in google_oauth_tokens across the project's life |
| 46 | 2026-08-29 | `GOOGLE_CLIENT_SECRET` was a well-formed secret belonging to a DIFFERENT OAuth client — Google checks the secret last, so it stayed invisible behind three other bugs | Trust (P0) | all mentors; token exchange failed after the mentor had completed the whole consent journey |
| 47 | 2026-08-29 | The Google health check searched followed HTML for `invalid_client`, so it scored `redirect_uri_mismatch` as healthy and reported all callback URIs registered while one was missing | Trust (P1) | mentors starting on the legacy PWA origin: Error 400 every time, with the diagnostic saying everything was fine |
| 48 | 2026-08-29 | The `security` workflow had failed on every commit to main — Semgrep runs diff-aware on a PR and full on a push, so no PR ever showed the 16 findings, and an always-red gate stopped being read | Playbook / Trust (P1) | no student directly; a weakened GCM tag check on session-handoff tokens and TLS verification disabled on the production DB script |
| 49 | 2026-08-29 | Ownership was bound to a person, so a departing rep's whole book would silently become unowned — `lead_outreach.owner_id` was `ON DELETE SET NULL` while `sales_followup.owner_id` was `ON DELETE RESTRICT`, two opposite outcomes for one event | Trust / sales ops (P0) | none yet — caught while both tables were empty |
| 50 | 2026-08-29 | One ceiling answered two different questions: portfolio assignment was gated on live-work capacity, so a seat could never hold more than ~200 students against an operating model of ~1,000 | Trust / sales ops (P1) | none yet — no book had been built |
| 51 | 2026-08-29 | The sales system had never held a lead: 974 students, `lead_outreach` = 0 rows, and no route could turn a student into one — both counsellors would have logged in to an empty queue on their first morning | Growth / sales ops (P0) | 2 counsellors, day one |
| 52 | 2026-08-29 | A counsellor tapping "Converted" deleted a student from every future queue with no payment anywhere — `call-queue.ts` treated the typed status as terminal while `/sales/leads` still showed them Active, so the surface they work from was the one that hid them | Trust / revenue (P0) | none yet — caught by audit while `lead_outreach` was empty |
| 53 | 2026-08-29 | The sales system could say what a counsellor LOGGED but never what they were GIVEN, so "did they work the right students?" and "how much of today is left?" had no answer — coverage had no denominator | Analytics / sales ops (P1) | none directly; the founder could not see leakage |
| 54 | 2026-08-29 | The call cadence engine had no upper bound: a `hot` lead who never answered was rescheduled for "tomorrow morning" regardless of how many times it had already been — 31 calls in 30 days to a student who never picked up once, landing hardest on abandoned-checkout students because they are the ones scored hot | Trust / student experience (P0) | none yet — caught by simulation the night before the first counsellor shift |
| 55 | 2026-08-29 | `/api/cron/outcome-sweep` was declared in `vercel.json` on 24 Aug and had NEVER run — the half of the learning loop that observes what a student did after an intervention, so every outcome column in `intervention_ledger` would have stayed NULL and the whole sales experiment would have produced no evidence | Analytics / experiment integrity (P0) | none directly; would have silently voided the 2-counsellor experiment |
| 56 | 2026-08-29 | `/api/cron/purge-session-handoffs` was declared on 27 Aug and had NEVER run, so the credential purge it exists to perform had removed nothing: 595 `pwa_session_handoff` rows, every one still holding an encrypted Supabase access+refresh token pair, all dead, oldest from 12 July | Security / data retention (P0) | none observed; a standing credential liability, not an outage |
| 57 | 2026-08-30 | `/sales` returned "This page didn't load" for BOTH counsellors — PostgREST puts `.in()` lists in the URL, so 975 students meant a ~38,000-character request and a 400 on every load. Seven reads passed the full list; only `lead_outreach` inspected its error, so the other six — including the paid-payments read that stops a paying student being dealt as a lead — failed silently | Growth / sales ops (P0) | both counsellors locked out of the product on their first morning |
| 58 | 2026-08-29 | `reconcile-payments` asks Razorpay what went wrong every 15 minutes, reads `status`, and discards `method`, `error_code`, `error_step` and `error_source` — the four fields that separate "our app cannot hand off to a UPI app" from "the bank declined a card". **CORRECTION (same day):** this entry originally led with "zero of eight orders from an installed iOS surface have ever been paid". That figure was built on a `distinct on (user_id, minute)` join that picked one arbitrary event per user-minute and merged in-app sessions with the browser sessions the app handed students to. Re-derived from the initiating session: installed-iOS is 11 orders and 0 paid, not 8; iOS Safari is 2 paid of 3, not 1 of 1; installed Android is 3 paid of 16, not 2 of 13. The instrumentation defect below is real and unchanged; the device numbers that motivated it were not | Payments / diagnosis (P0) | the product's most expensive open question was unanswerable from our own database; no student harmed, no money lost |
| 59 | 2026-08-29 | CareerRai serves from two live origins and Razorpay accepts payments from only one: 13 orders minted from `careerrai-daily.vercel.app` — 0 paid, 2 refused outright with `source=internal` "Payment blocked as website does not match registered website(s)" — against 20 from `careerrai.in`, 5 paid. One student was blocked twice on the legacy origin and paid nine minutes later on the canonical one. 101 students used the legacy origin in the last 7 days | Payments / conversion (P0) | between 0 and 13 lost orders; only the 2 refusals are certain, the other 11 never produced a Razorpay attempt at all |
| 60 | 2026-08-30 | Incidents #55 and #56 were the SAME defect and neither was a scheduling problem: Vercel Cron invokes with GET, `outcome-sweep` and `purge-session-handoffs` exported only POST, Next answered 405, and because `withCronTracking` sits inside the handler no `cron_runs` row was written — making "called and rejected every day" identical to "never scheduled". Of 41 crons exactly those two lacked `export { POST as GET }`. Underneath it, `pwa_session_handoff.payload` was NOT NULL while the purge sets it to NULL, so the scrub failed on all three runs it did get | Infrastructure / learning loop (P0) | the learning loop produced no outcome data for 6 days; 595 encrypted access+refresh token pairs retained since 12 July |
| 61 | 2026-08-30 | The forensics probe read the forced-login visit correctly all along — `no_marker`, both stores gone, `persistedBefore` true->false, i.e. origin eviction. Nobody saw it because on `/login` the student is not signed in, so the event lands with `user_id` NULL and every query filtered by his user id excluded it; the rows that DID carry his id were taken after the probe re-armed its own markers and read `all_intact, age=0`. Three wrong diagnoses followed, including asserting `track()` had no unload flush when it flushes on `pagehide` via `sendBeacon` | Diagnostics (P1) | two days of a live student's forced re-logins mis-attributed to cookie loss instead of eviction; no student harmed |
| 62 | 2026-08-30 | "Continue with Google" (primary CTA on `/start` from 29 Aug) could only ever create a SECOND account: Supabase links a Google identity to an existing user solely on a confirmed email match, and 963 of 969 students have no email on file. All 5 Google accounts created in two days had no phone identity; one is a real onboarded returning student who can never be reached. The duplicate-account guard in `/auth/callback` had never executed — gated on `!existing`, which the `handle_new_user` trigger makes always false. Also found: the onboarding Mobile field was a client write over the verified phone (92 rows), and the "54 students with no phone" were abandoned OTP requests, not students | Identity/Auth (P0) | 3 real students unreachable forever; every existing phone student was one tap from a duplicate account with no streak, plan, buddy or payment history |
| 63 | 2026-08-31 | The Daily Pick promoter could never stamp a RECYCLED item. `promoteDailyPick` guarded its write with `.is('featured_on', null)` — true only for a never-featured row — while `pickForKind` deliberately returns a previously-featured row once fresh stock is exhausted (daily-pick.ts rule 5, "the shelf cannot run dry"). The update matched zero rows, nothing was stamped, and the surface would have rendered empty from the day the shelf emptied. Latent for 10 days because fresh stock had never actually run out; found while making Daily Pick hint-only, which cut the shelf from 89 items to 38 with only 4 never featured — four days from blank. The guard's real intent was first-writer-wins for TODAY, not never-again | Community / Daily Pick (P1) | none — caught before it fired; would have blanked the daily surface for every student within 4 days |
| 64 | 2026-09-01 | `notif_prefs.push_prompted` is written ONLY on decline and ONLY by `push-gate.tsx`, which renders ONLY on the buddy/staff layout — the student ask never writes it at all (`grep -c` = 0). Two sessions hours apart each read its absence as "never prompted" and produced a population number (~631, ~704) that was never a measurement. The trap is baited by `notif_prefs` having a DB DEFAULT of `{"push": false}`, so every profile is born looking like a refusal. Known since 15 Aug but recorded only in an admin UI string and a code comment — neither is where someone querying the database looks. Fixed by instrumenting the ask itself (PR #159): `push_ask_skipped.why` now separates ios_wrapper / not_standalone / unsupported / already_granted | Diagnostics / Notification (P1) | none — both diagnoses caught before any code changed; cost was two wasted investigations |
| 65 | 2026-09-02 | The Command Center said STUDENTS 1000 for three days while 1,036 real students existed. PostgREST caps every response at `max-rows` (Supabase default 1000) and returns the first thousand rows of an unbounded `.select()` with NO error; `getRealStudents` counted what it was given, and every dashboard card derives from that list. A client `.limit(20000)` does not help — max-rows applies after it. The same shape existed in 134 reads across the operator surfaces and crons; several were already truncated for weeks: student_events (237k rows), notifications (100k), topic_coverage (50k), funnel_events (19k), decision_log (4k), daily_routines (1.9k), student_dna (1,038). Every cron that iterated "all students" skipped whichever ~36 sorted last under heap order — a set that is not stable across days. Fixed with one paging helper (`lib/supabase/fetch-all`, explicit order + short-page termination + loud ceiling), `readAllRows` in the truth boundary, 88 reads migrated, and a shrink-only guard | Founder visibility (P1) + student correctness (P0) | ~36 real students invisible to every roster-driven cron and dashboard for at least a week; seven admin metrics silently reporting a fraction as the whole |
| 66 | 2026-09-02 | The sales book was a one-time load, not a pipeline: 124 `lead_outreach` rows written by the manual enrolment on 29 Aug and never added to, while 916 real students (172 of them signed up in the last seven days) had never entered any book. Nothing ran daily; the Phase 2A intake engine had been designed and deliberately deferred, and no surface said the intake had never happened. Also found: all 124 rows had `assigned_at` NULL, so the first-contact SLA had been 'unknown' for the whole book since day one, and `newToday` was hard-coded to 0 so the daily fuse could never bind | Sales (Trust) (P1) | every student who signed up after 29 Aug — no counsellor could see them |
| 67 | 2026-09-03 | The daily list was never closed: 240 of the 241 cards ever dealt to a counsellor were still open, back to 30 Aug. A card left the deck ONLY on a logged call, so `worked_at is null` stored three different facts at once — never got to it, deliberately deferred, could not act — and there was no honest way to close a card without claiming a call happened | Sales (Trust) (P1) | every student dealt and never marked; the founder could not tell a worked day from an ignored one |
| 68 | 2026-09-03 | Two defects in the day shipped that morning, found by reading production the same night. (1) The queue is rebuilt on every page load, so a worked card left and rotation backfilled to the floor — each seat was offered **97** cards against a ceiling of 70 and the list could never be finished. (2) The end-of-day sweep ran `ist_day < today`, so at 21:45 IST it closed nothing: today's cards sat open until the following night and the founder's "unmarked" column always reported yesterday | Sales (Trust) (P1) | both counsellors, every day — an unfinishable list and a day-late record |
| 69 | 2026-09-04 | The calling card's "Last time" block — the one thing that makes a second call better than a first — was mostly not what the student said. `call-queue` read `sales_activity` with no provenance filter and kept the single newest row, but lead intake writes its own bookkeeping into that same table: the newest row for **272 of the 319** touched students was `"Daily intake -> Neelam Singh: 50 of 50 new-per-day used..."`, against 47 with a real human touch. Compounding it, `no_answer` (the commonest disposition, and auto-noted `"Did not pick up"`) buried the real conversation from the call before it. Fixed with `lib/sales-remarks.ts` as the one definition of a remark (`provenance='self_reported'` AND `actor_id IS NOT NULL`), filtered at the database, leading with the newest TYPED remark and keeping the newest five | Sales (Trust) (P1) | both counsellors, on every repeat call — the student's own words replaced by our internal lead-distribution ledger |
| 70 | 2026-09-04 | OTP delivery stopped at 09:00 UTC and ran broken for seven hours with nothing to show it. Supabase called `/api/auth/sms-hook` 18 times, the hook returned 200 every time, and not one SMS arrived: 31 requests produced a single verification attempt, 13 more were rate-limited as students resent, and 0 students verified against 12/12 the day before. No code was at fault — the OTP path had not been touched in weeks and the phone normalizer is correct. `indiahost-otp.ts` decided whether a send had worked by hunting for five error words in the gateway's reply and treating everything else as success, so "insufficient balance", "account suspended", "quota exceeded", "number blacklisted" and an empty body all read as delivered. Fixed by recording the gateway's reply on every send, checking rejection before success, returning 500 on a rejection so Supabase surfaces it, and reporting an unclassifiable reply as UNKNOWN rather than guessing it either way. Delivery resumed on its own at 4 Sep ~19:00 UTC after ~10 hours, confirmed by student_events, three real sessions in auth.users, and codes arriving on a handset — but **the provider never explained the cause and offers no delivery reports**, so an unexplained outage can recur without warning. Second, separate failure: nothing alarmed for seven hours, because `founder-alerts` only sees per-student states and a student who cannot log in never becomes a row — a per-entity alert system is structurally blind to a platform-wide fault. Closed by `src/lib/os/auth-health.ts`, an OUTCOME check (codes requested vs codes verified over 60 min), because every layer we control reported success throughout | Identity/Auth (P0) | every student and the founder — nobody could sign in for seven hours, and nothing in logs or dashboards said so |
| 71 | 2026-09-04 | The conversion lane was a permanent FLAG, not a signal: `buddy_cta_clicks` never resets, so 136 students sat in "buddy interest" forever — only 32 had tapped within a fortnight and the oldest live one was 21 July. It took 47 and 52 cards of a 70-card day, rotation got **zero**, and 243 never-contacted students were dealt nothing, while the card told the counsellor "intent is warm" | Sales (Trust) (P1) | 243 students unreachable that day; ~94 pitched on six-week-old intent |
| 72 | 2026-09-05 | The counsellor's day refilled itself all day. The first assembly of 5 Sep was exactly right — 65 cards, attention at its ceiling of 20, new arrivals at 15 — and by 22:00 the two seats had been dealt **111 and 174** against a ceiling of 70, attention alone at 45 and 64. Every ceiling (each lane's, the day's, rotation's target) was measured against the cards still ON SCREEN: work a card, it leaves the queue, the lane has room again, and the next page load deals a fresh full allowance. **Incident #68 found this exact defect and fixed it for rotation alone**; the same hole was left open in every other lane and in the day ceiling itself. Separately the sweep closed the day at 21:45 and the deck dealt 20 more cards at 22:00. Fixed by making `DayContext.usedToday` the day's ledger — cards DEALT today in every state — measuring every ceiling against it, and dealing nothing new past the shift | Sales (Trust) (P1) | both counsellors, every day since 2 Sep: a list that could not be finished, and coverage that read 25/111 and 95/174 |
| 73 | 2026-09-07 | Production was eight days from going read-only and nothing said so. The database sat at **379 MB of the free tier's 500 MB**, growing **~15 MB a day** — at that rate writes start failing around 15 Sep and students cannot log study. It surfaced only because a Supabase email about an unrelated EMPTY test project prompted a look at the org. 290 MB of the 379 was telemetry: `student_events` 196 MB, `notifications` 73 MB, `perf_events` 21 MB. Half of all events were `tap`, which **nothing in the repo reads** — the only mentions outside the writer are the event-name union and a FORBIDDEN_KINDS list that exists to keep it off the student's timeline. Fixed with a per-event-name retention sweep whose default is KEEP, rails in the DATABASE (two tables only, an explicit event list required for student_events, cutoff >= 7 days, service_role only), and a guard test that fails the moment a swept event acquires a reader. 145,125 rows removed; the learning loop (22,304 `app_open` back to 15 July) untouched | Platform / Analytics (P1) | every student, 8 days out — a full read-only outage of study logging |
| 74 | 2026-09-09 | A re-dial was classified as a promise, so the no-answer pile ate whole days and fed on itself — every unanswered call manufacturing tomorrow's card. `retry` sat in UNTRIMMABLE beside `callback`, inheriting "promises are never bumped" (2 Sep). One counsellor's retry lane ran **60, 51, 73, 83** across four days; on 9 Sep she was dealt **116 cards, every one a promise**, worked 72 and left **44 unmarked** — the first list since the counting became honest that could not be finished — with **zero never-contacted students for the fourth day running** against 250 in her book. It put two founder rules in conflict: promises are never bumped (2 Sep) vs every list must end marked (3 Sep). Resolved for the harder rule: a callback is a commitment a STUDENT extracted from us and stays untouchable; a retry is our own policy and takes `RETRY_CEILING` (20). The day's ledger was also re-keyed from SECTION to LANE, because a per-section cap would have silently bumped callbacks sitting beside the retries. Replayed on that day: 116 cards become 60, and rotation reaches 15 | Sales (Trust) (P1) | both counsellors; one worked a 116-card unfinishable list, and ~250 never-contacted students went unreached for four days |
| 75 | 2026-09-02 | careerrai-test raised `rls_disabled_in_public` a SECOND time. The 26 Aug parity migration enabled RLS on the 91 tables that existed then — it could not protect `public._probe`, created afterwards by an ad-hoc probe run with RLS off and full `anon` grants. Fixing the census never fixes the default. Dropped the scratch table and installed an event trigger that enables RLS on every new `public` table, verified with a positive control. Production was never affected (101/101 RLS-enabled, checked read-only first) | Security (P0 class, zero actual exposure) | 8 rows of probe pass/fail strings were world-writable on a test project; no PII, no credentials, no production data |
| 76 | 2026-09-15 | Three students said the plan did not match their coaching. The plan engine was right — a saved timetable owns the day; the input was missing. **17 of 357 coaching students (4.8%) have ever saved one**: 280 dismissed the ask, 62 tried, **46% of tries failed**. Of those, 23 × 422 across 12 students over seven weeks were undiagnosable: `api/timetable/parse` had **four ways to answer 422 and recorded which one to nothing** (client logged `{status}` only; production holds zero `ocr_failed` timeline rows, so the branch that does log was never the one hit). Worse, three of those messages ended *"or add your classes by hand"* — **a screen that has never existed anywhere in the product**. Fixed: `lib/timetable-refusal.ts` names all eleven refusals with stable codes and unchanged statuses, splits `not_a_timetable` from `model_reply_unreadable` (a photo problem vs our own extractor), returns `{error, reason}`, and a guard test fails the build if a refusal message ever names a phantom door again. Still open: 340 coaching students have no door in but a file upload | Learning (P0) | 12 students hit an unnamable dead end over 7 weeks; 340 coaching students study against a plan that has never seen their class schedule |
| 77 | 2026-09-15 | Asked to surface "38 untouched Tier-A students" — **the 38 did not survive checking** (only 6 of 79 habit-formed students had never been dealt a card). The real number was larger and unrelated to tiers: **609 of 1,138 students in the two books had never once been dealt a card**, Neelam 333 of 583 and Anshul 277 of 555. Worse, never-contacted cards dealt per day ran Anshul 14-47 while **Neelam got 0 on nine of ten days**. Mechanism: `assembleDay` treats `ROTATION_FLOOR` as a floor but applies `Math.min(room, …)` after the untrimmable lanes are counted, so a counsellor who books many callbacks (35 of her 73 cards) starves the one lane with no claim on the day — and the more promises she makes, the more completely it starves. **Incident #74's sibling: capping `retry` at 20 just moved the starvation to `callback`, which has no ceiling by design.** Not fixed by forcing cards in (SALES-OS §5; she already had 30 unkept callbacks) — reported as one Exception `book_never_reached` that skips non-working days so approved leave never reads as starvation **Corrected hours later:** v1 counted cards DEALT; over 14 days Anshul was dealt 229 cold cards and worked 65, Neelam dealt 44 and worked 1 (promises: 91% and 74%), so the reading now counts students actually WORKED — a card dealt is not a student reached | Sales (Trust) (P1) | 609 students — over half the base — had never been contacted once; 333 of them in one book that had moved zero of them for nine days |
| 78 | 2026-09-15 | Asked to split Neelam's book so every student gets tapped; measurement said the book was not the problem (583 vs Anshul's 555). Her DAY was **100% promise cards** — 0 never-contacted students dealt for nine working days, while 333 of her 583 had never been dealt anything. Mechanism: `UNTRIMMABLE` holds `callback`, and **an overdue promise is redealt every morning until worked with nothing ageing it out**, so 24 overdue promises (15 past 3 days, 10 past a week, oldest 7 Sep) commit 24 of ~70 slots before the day begins. **Splitting the book is neutral** — the debt travels with the promises, not the book size — so the exception's own sentence says so where the founder reads it. Third in a family: #74 capped `retry`, #77 found the starvation had moved to `callback`, #78 finds `callback` unbounded because nothing expires an unkept promise. Reported as `promises_overdue_blocking_day`; the action is "clear", never cancel/bump/expire (asserted by test) — a promise a student is owed is not ours to delete for our own throughput | Sales (Trust) (P1) | 333 students in one book never dealt a card; 24 students promised a call still waiting, the oldest 8 days |
| 79 | 2026-09-15 | Went looking for premium students with no owner; both HAVE one (the same mentor) and sales excludes payers deliberately. The real fault: **both paying students had received zero sessions.** Arnav booked 5 between 9-22 Aug — **4 expired, 1 cancelled, none ever started or ended**; Monu had 1 request that never became a session. Both then stopped studying (last logs 4 and 6 Sep). Nothing fired because sacred alert 2 is `.is('buddy_id', null)` — "paying student with NO mentor" — so **assignment was being counted as delivery**, the same wrong-noun audit as #75 and #77. Added a fourth sacred alert: premium + mentor + no `video_sessions` row with `ended_at`, 7+ days past `premium_since`. Delivery is defined as a session that ENDED, not one booked — filtering on `session_status` would need every failure word, and the positive fact cannot be fooled by a status nobody thought of. Unknown `premium_since` counts as overdue, never fresh. Also fixed: `classifyObjective` would have labelled a paying student CONVERSION ("this is a commercial conversation") — `alreadyPaying` now wins above every commercial signal **CORRECTED SAME DAY: the sessions WERE delivered, on time — they were never closed out.** Reading `expired` as undelivered is the inference `release-stale-sessions` exists to refuse (it documents `expired` as "nobody recorded an outcome" and won't write `cancelled` because that "asserts it did NOT happen"). Twin of #77 — *an empty record is not an empty service*; both times I read our bookkeeping as an observation of the world. Alert now reports only "we cannot say what this student received", severity high not critical. **Still open:** `expired` is terminal under two triggers, so the cron's promise that "a mentor can still mark it completed afterwards" is false — delivery can never be recorded after expiry, and `completionRate()` is permanently understated | Trust (P0) | delivery records for 2 paying students are unfixable; a mentor was named in an alert for work she had done |
| 80 | 2026-09-15 | Founder said he did not trust the daily-log numbers; he was right. Weekly rows ran 81→140→124→**170**→150 and were wrong three ways at once. (a) **Half of all "logs" record ZERO study** — the best week, 31 Aug, was 91 zero-hour rows out of 170. (b) **Ticking a plan task writes the same row type** as the daily-log form (`complete-task` → `upsert_log_and_streak`); ticks went 26-48/wk → 187-211/wk, so one number counts two very different actions. (c) **The rise was volume**: signups 169/324/218/164/78 → 2 with ads off, logs trailing one week, and 117 of the 140 logs on 17 Aug were students under 8 days old. Per cohort, "studied in week 1" is still BELOW the 20 Jul cohort (17.7% vs 20.8%). The one real gain is habit: 3.0% → 7.3%. Same mistake as #77 and #79 — **counting the record instead of the thing** — but undetected for a month because the number was going UP. Fixed with `lib/os/study-truth.ts` in the founder digest: counts students who studied not rows, shows the subset past week one, carries the zero-hour caveat, and reports habit only for cohorts whose 21-day window has CLOSED | Analytics (P1) | a month of decisions taken against a number that mostly measured ad spend |
| 81 | 2026-09-15 | Founder pushed back: *"students are active and responding... they are mentioning ki we are not able to study"* — and he was right. With ads at ZERO for a week: **160 active (155 of them 8+ days old), 42 studied, 51 explicitly recorded "did not study", 102 opened and logged nothing.** Mature weekly actives 35→58→95→143→**155**, rising again with no ads (flat ~13% as a rate against a pool that grew 272→1203 — retention did not improve, but did not decay while volume tripled). Two faults found: (a) the attention lane asked `logDates.some(...)`, and a row exists whether the student studied OR said they could not — so **every student who answered honestly was counted as having studied and dropped from the queue**. Fourth instance of counting the record not the thing (#77/#79/#80), and the worst, because it silenced the ones who replied. `studiedDates` added beside `logDates`; declared answers get their own label and outrank the merely-seen. (b) **Only 703 of 1,208 are sitting CAT 2026** — 201 are 2027, 299 never set it (10 active, 1 studied) — and `call-queue`/`lead-intake`/`sales-day` had no reference to `attempt_year` at all, so this year's scarce calls went at equal priority to next year's students. `sales-attempt-year.ts` weights discretionary lanes only; **promises are never reordered** | Sales (Trust) (P1) | 51 students who answered honestly were unreachable by the deck; this year's call capacity split across 1,208 when only 703 sit this year |
| 82 | 2026-09-15 | Founder asked of the Command Center: *"are these numbers real?"* Two were not. (a) **`getSalesReadyToCall` computed the already-called exclusion and never applied it** — it returned `rows` (all flagged) instead of `ids`, so the card read **1,098 when 423 had already been called and the true number was 675**, pointing two counsellors at students they had spoken to. Its read was independently broken: one `.in()` over ~1,100 ids against **1,411** matching rows (past the 1,000-row cap, URL long enough to be refused) with `error` DISCARDED — now chunked, paged, and throws, the rule `getRealStudents` states ten lines above. (b) **Three tiles labelled "Studied"/"Active" all counted log rows**: "Studied yesterday 29" when 11 studied, "Active this week 67" when **166** opened the app — overstating study 2.6x and understating activity 2.5x on one screen. Relabelled to "Logged", since count and list must come from the same filter and the People page derives activity from logging too. Fifth instance today of **using the name of the record as the name of the thing** (#77/#79/#80/#81) | Analytics (P1) | 423 students re-queued for calls they had already had; founder reading a screen that was wrong in both directions |
| 83 | 2026-09-15 | Asked how to get the 675 uncalled students called, counted them first: of **401 CAT-2026 never-called students, 15 were in the app this week, 113 this month, and 220 had opened once and never returned** — and the deck dealt all of them in the same breath. Cause: the fresh lane ranks on `scoreConversion`, whose only recency signal is `activeRecently: daysSinceLastLog <= 3` — **logging**. Neither the alive nor the dead group logs, so both scored zero and the one signal separating them (`profiles.last_seen_at`, already loaded) was never read. Matters because a call to a student alive-but-not-logging revived them at **11.5% vs 0.8%** on a matched control — ordering 128 ahead of 220 beats any change of script. `lib/sales-liveness.ts` adds a bounded ordering term from `last_seen_at` (7d > 21d > older); it only ever LIFTS the present, never demotes the quiet, and touches discretionary lanes only. Sixth instance in one day of naming the record instead of the thing (#77/#79/#80/#81/#82) | Sales (Trust) (P1) | the 128 reachable students were queued behind 220 who had not opened the app in a month |
| 84 | 2026-09-15 | Founder asked where the buddy CTA funnel drops. It had not dropped, it had **stopped**: `buddy_nudge_shown` went 16 → **0** on 1 Sep and stayed there for two weeks, and `promo_impressions` modal channel went 37 → 12 → **0**. That surface is the best commercial instrument we have — **28 of 124 shown reached the CTA (22.6%)** vs **28 of 3,458 evening pushes (0.8%)**. Nothing recorded why, because `DailyBuddyNudge` had **six ways to bail and one observable outcome: silence** — a surface that can only report success cannot report that it has stopped. Instrumented rather than guessed (founder: *"pehle instrument karke exact wajah dikhao"*): `buddy_nudge_mounted` before any gate, `buddy_nudge_blocked.gate` on a closed union. Separately, `buddy-evening` was spending the shared daily pitch slot on students **already in the app** (NOTIFICATION-OS §10b.1) and now stands down for them — a watch, not a win, because that slice clicked at **3.17% vs 0.48%** and only pays once the modal is firing again | Growth / Notifications (P1) | two weeks of the 22.6% surface showing to nobody, found by a founder's question and not by the system |
| 85 | 2026-09-15 | Founder: *"jis bhi student ne ek se zyada din log kiya hai wo pehli priority hain calling ke liye"*. Counted first: **103 free students have logged on 2+ separate days in the last 30, and 47 of them have studied nothing in 15** — `classifyLane` returned **`null`** for them, i.e. backlog. Every retention lane required a HABIT to already exist (`going_cold` 3-of-7, `broken_streak` a 5-day run, `new_never_logged` zero logs), so the lanes covered the strong end of the distribution and classified its bigger tail as nothing. Day one is onboarding carrying the student; **day two is the student choosing, on a different day, to come back** — the only unpaid evidence of intent the free product produces (186 logged exactly one day; 103 logged two or more). Added the `restart` lane, band 3.25M: below the two habit lanes, above everything that is not a promise, dealt as a CALL with a retention objective. It respects `TOUCH_COOLDOWN_DAYS` **unlike the other retention lanes**, because two logged days never expire and an exempt card would be re-dealt every morning — the #74 deck that put 115 of 121 worked cards into the next day. Direction-only evidence, said as such: 8.3% (4/48) revival vs 4.1% (5/123), which is four revivals and not proof. Also verified the deck now refreshes: worked→re-dealt-next-day ran 115/121 on 8 Sep and is 1/77 on 15 Sep | Sales (Trust) (P1) | 47 students who had shown us the product works for them were queued as backlog |
| 86 | 2026-09-15 | Asked to confirm both reps get **70 students daily and that they refresh**. Refresh was fine (worked→re-dealt-next-day is **1 of 77** on 15 Sep, since #74's fix) and callbacks were clean (46 set: 0 with no clock, 0 where the clock disagreed with the promise, 0 wrong status). The seventy was not. Cause, one word in `assembleDay` — `Math.max(ROTATION_FLOOR, DAY_FLOOR - signalsToday)`. It decided two things nobody chose. (a) **The deck a counsellor OPENS was exactly DAY_FLOOR**: the first build of Anshul's day was **50 cards on 10, 11, 13, 14, 15 and 16 Sep** (53 on the 12th). Day totals of 59-73 came only from signals added LATER, never bound by this target — his morning was fifty cards, and on 14 Sep he WORKED 65. (b) **The never-contacted share was frozen at whatever signals existed when the page was FIRST opened**, since `usedRotation` has spent the target by then: same rep, same book, same week — **06:50 → 47 fresh, 00:07 with the retry lane full → 27, 02:16 → 15** — while **319 never-contacted students** sat in that book with phones (Neelam 387). How many students got a first conversation was set by the clock time somebody happened to load a page. Fixed to `DAY_CEILING`; ceiling, lane caps, the #72 worked-card rule and SALES-OS §5 (short is reported short, never padded) all unchanged. **Four tests had the old rule as their expected answer** and two more carried a literal `60` as "the ceiling", stale since DAY_CEILING became 70. **This entry's own first draft said "the day ended at exactly 50, every day" — wrong, corrected the same night: a daily total hid what `min(surfaced_at)` showed plainly** | Sales (Trust) (P1) | a fifty-card morning for months, and between 15 and 49 first conversations a day decided by page-load time |
| 87 | 2026-09-16 | Founder at 09:36 IST: *"why these daily logs decreased so significantly suddenly?"* **LOGGED YESTERDAY 12** against a remembered **30**. Nothing had decreased: students fill in the previous day's log during the NEXT day, and only **22% of that backfill arrives before 10:00 IST** (78% later, peaking 19:00-21:00). Split by when the row was WRITTEN: 14 Sep was 11 same-day **+18 backfilled = 30**; 15 Sep was **9 same-day +3 so far = 12** — and nine is the most ordinary number in that column all week. A day this age is typically **58% complete**, so 12 implied ~21, in line with 19/21/23/23. The "rise" it was compared against was not one either: 31 settled days have mean **22.1**, SD **5.9**, range 9-32, so 19-30 is ordinary scatter and naming a cause would have been a precise lie (L1). Ruled out first, with evidence: traffic (46 opened the app on 15 Sep, the week's highest), calls (4-12 logs/day from students called within 48h, no relationship to the total), signups (a 2-week slope, not a 1-day step), code (`main` unchanged 9-15 Sep). `lib/os/log-maturity.ts` gives the two still-filling tiles their own measured maturity and shows the last SETTLED day beside them; the note sits **on the tile**, because the eye compares two figures before it reaches a caption. Tiles deliberately NOT re-pointed at a settled day — `ActivityState` derives from days-since-log, so the count and its People list would have stopped agreeing | Analytics (P1) | a founder reading a collapse into a number that was two-fifths unwritten, every morning |
| 88 | 2026-09-16 | Neelam, on WhatsApp: *"aapne kl bola tha 40 se jada mt krna is liye nhi kiye mene because mai msg krti hu to vhi total no. count hote h ese me to mai confused ho re hu ki kitni call ki h kitni nhi"* — **she stopped short of her day's work because she could not read her own tally.** `CallDeck` held ONE `done` counter incremented on every disposition, so a message, a skip and a connected call all moved the same number. Second defect in the same message: *"agar vha pr not pick walo pr filter lga skti to"* — 70 cards arrive as one list, and the 18 who had not picked up could only be found by reading all 70. `lib/sales-deck-filter.ts`: the tally splits **called · messaged · skipped** (an unanswered dial IS a call — she rang; a skip is neither, since it writes no lead state), plus five chips cutting ACROSS the sections (All / To call / To message / Didn't pick up / Never called), counted from `no_answer_count` not the `retry` lane, sized from the whole day not the filtered view (#72), and hiding cards from the VIEW never from the day. **No target, goal or progress bar** — SALES-OS §0 forbids a P5 number becoming a quota, and a guard test fails the build if one reaches that header | Sales (Trust) (P1) | a counsellor's afternoon; the first operator number that was TRUE and still unusable |
| 89 | 2026-09-16 | Asked for the solution to *"maximum conversion, zero mixup between reps, maximum students outreach"*. All three had one measurable answer. Over 30 days and 1,057 worked cards, **433 — 41% of every call the business made — went to `retry`**, which connects at **11.5%** and yields **1.4% interested**, against `fresh` at **38.1%** and **7.1%**: a never-contacted student answers **3.3x** more often and is **5x** more likely to be interested. Meanwhile **673 of 1,209 students have never been dealt a single card**, and 30 days of cards (2,265) went to only **537 distinct students**. Not a judgement of anyone — the queue hands out those cards; on 16 Sep one rep worked 20/20 retry, 7/7 followup, 1/1 callback and **zero of 37 fresh**. Fixed: `RETRY_CEILING` 20→12 and `FRESH_PIN_PER_DAY` 5→25 (the 5-card test had passed — 5/5 worked on 14 Sep). **The pinned block moved BELOW the promises**: at 5 it cost a student minutes, at 25 it would cost a ~28-card rep every promise in the day, and one counsellor carries 35 callbacks — founder chose the size, the Constitution the order. Mixup was real and measured: **86 students dealt to BOTH reps on the same day** (0 worked by both — slots spent, not trust), cause `canAccessLead` answering true for unclaimed; it stopped 7 Sep only because 72 of the 75 still-unowned students have no phone. `lib/sales-unclaimed-owner.ts` now deals an unclaimed student to one seat by stable hash, failing toward nobody. **No conversion tuning** — the business has 2 conversions total and optimising on n=2 would be fabrication | Sales (Trust) (P1) | 41% of all calling effort aimed at the worst lane, indefinitely, while half the base was never called |
| 90 | 2026-09-16 | Asked to automate assignment "smartly, daily". It already was — `lead-intake.ts`, 14 runs in 14 days, 0 students outside a book — so the real question was what assignment was failing to do. **Said first, plainly: assignment cannot fix the constraint.** Books 554/583 against 218/185 distinct students worked in 14 days means **one full pass takes 36 and 44 days**; no algorithm changes that, it only changes who waits. Two real gaps found. (a) **Absence stranded a whole book**: Neelam away 12-14 Sep, her **583 students got nothing for three days**, and nobody else could reach them because an owned lead is invisible to every other seat; `unavailable_until` stops INTAKE but does nothing about the book already there, and no leave was ever recorded anyway. (b) **The split levelled the wrong number**: apportioning by remaining allowance kept book sizes level (554/583, 5% apart) while never-contacted drifted to **319/389, 22% apart**. Fixed: `lib/sales-absence-cover.ts` (declared leave OR 2.5h of shift with zero cards marked) grants **access for the day only** — no ownership moves, nothing written, guard test forbids the module containing `update`/`insert`/`owner_id`; an absent book goes to exactly ONE cover by the same deterministic pick as #89; `canAccessLead` gained `coveringRepIds`, absent by default. `planIntake` now levels **backlog per unit of allowance**, so capacity still shapes the share (2A §5 step 6) and equal-capacity seats level the wait. **Not built: routing by fit** — 2 conversions total, any model on that is a guess (L1) | Sales (Trust) (P1) | 583 students unreachable for three days, invisible to every surface |

> Entries 12 and 13 were never written. The gap is left visible rather than
> renumbered — the numbers are referenced from commit messages and code
> comments, so closing it would break those references.

---

> **This file is the INDEX.** The full entries — symptom, root cause, cost,
> prevention — live in `ENGINEERING-MEMORY-ARCHIVE.md`. Before building in a
> subsystem, scan this table for incidents touching it, then grep the archive
> for `## Incident #<n>` and read only those. (Split 12 Aug 2026: the one file
> had grown past 1,000 lines and was being re-read whole on every task.)
>
> **Adding an incident:** full entry in the archive, one-line row here — same
> commit, next number.

---

## CareerRai engineering laws

Laws outrank incidents: an incident teaches one lesson, a law refuses a whole
class of them. Both are binding.

**L1 — A trustworthy UNKNOWN is infinitely more valuable than a precise lie.**
(Founder, 18 Aug 2026.) CareerRai Notice only works if the student comes to
believe *"CareerRai notices things about me I didn't notice myself."* That
belief survives a silence. It does not survive one discovery of *"wait —
CareerRai made that number up."* Insufficient evidence produces UNKNOWN or
silence, never a weaker guess, never a default, never a flattering one.

**L2 — No claim about product behaviour from code location alone.**
(Founder, 18 Aug 2026.) Trace PRODUCER → WRITE → CONSUMER → SURFACE → REAL
DATA before asserting what the product does. Every 0C.3 investigation found a
defect this way that reading the code had missed — and one investigation found
a defect in its own first pass the same way.
---

## How prevention becomes permanent

An incident is only closed when its lesson is encoded somewhere with teeth — a
Constitution non-negotiable, a shared library that makes the wrong thing
impossible, or a Playbook gate. A lesson that lives only in this file will be
repeated; a lesson wired into `push-client.ts` or `admin-filters.ts` cannot be.
