import { CatReadinessClient } from './cat-readiness-client';

export const metadata = {
  title: 'CAT Readiness Test — CareerRai',
  description: 'Free 5-min test. Know exactly where you stand before CAT.',
};

export default function CatReadinessPage() {
  return <CatReadinessClient />;
}
